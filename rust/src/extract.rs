//! Document extraction. Ported from `src/extract/{kreuzberg,mime-filter}.ts`
//! in Phase 5. The TS-only `pdf-slice.ts` and `wasm-console-filter.ts` are
//! deleted, not ported (Kreuzberg native handles their roles).
